# Machine-generated files. Marking them keeps diffs collapsed in review and
# excludes them from language statistics — they are regenerated by tooling, not
# edited by hand.
#
#   src/routeTree.gen.ts  — @tanstack/router-plugin, regenerated on dev/build
#   drizzle/meta/**       — drizzle-kit schema snapshots, regenerated by db:generate
#
# The .sql migration files are intentionally NOT marked: they run against
# production data and deserve line-by-line review.

src/routeTree.gen.ts linguist-generated=true
drizzle/meta/** linguist-generated=true
