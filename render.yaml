services:
  - type: web
    name: stride-course
    runtime: node
    plan: free
    region: ohio
    buildCommand: npm ci && npm run build
    # Render's pre-deploy command is not available on free web services.
    startCommand: npm run db:migrate && npm start
    healthCheckPath: /api/health
    autoDeployTrigger: checksPass
    previews:
      generation: automatic
    envVars:
      - key: NODE_VERSION
        value: "22"
      # Service previews copy this connection string from the base service.
      # Keep this database disposable and free of production or sensitive data.
      - key: DB_URL
        fromDatabase:
          name: stride-course-db
          property: connectionString

databases:
  - name: stride-course-db
    plan: free
    region: ohio
    databaseName: stride
    user: stride
    # The app and preview instances connect over Render's private network.
    ipAllowList: []
